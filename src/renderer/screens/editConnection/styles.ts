import { styled } from '@mui/material';

export const Container = styled('div')(() => ({
  maxWidth: 1200,
  margin: '0 auto',
  display: 'flex',
  justifyContent: 'center',
  flexWrap: 'wrap',
  gap: 20,
}));
